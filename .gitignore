build
# Keep the project-local vinext "sites" plugin that vite.config.ts imports.
# Without this exception the generic `build` rule above swallows it, breaking
# `vinext build` on every fresh checkout (CI + AWS deploy).
!/timewatcher-platform/build/
!/timewatcher-platform/build/**
dist
docs
other
old

# Coverage
*coverage*
htmlcov

# Editor/IDEs
.idea
*.swp

# Python
*venv*
__pycache__
.python-version

# Misc
.*cache
.DS_Store
